<a data-url="{{ $url }}" data-id="{{ $id }}" data-flg="{{ $flg ? 0 : 1 }}"
    class="btn flg-change-btn {{ $flg ? 'btn-light' : 'btn-secondary' }} {{ $addClass }}">{{ $flg ? ___('mycustom.word.true') : ___('mycustom.word.false') }}</a>
